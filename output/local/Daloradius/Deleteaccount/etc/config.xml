<?xml version="1.0" encoding="UTF-8"?>

<!-- The root node for Magento module configuration -->
<config>

    <!--
        The module's node contains basic
        information about each Magento module
    -->
    <modules>

        <!--
            This must exactly match the namespace and module's folder
            names, with directory separators replaced by underscores
        -->
        <Daloradius_Deleteaccount>

            <!-- The version of our module, starting at 0.0.1 -->
            <version>0.0.1</version>

        </Daloradius_Deleteaccount>

    </modules>

    <!-- Configure our module's behavior in the global scope -->
    <global>

<!-- Defining models -->
        <models>

            <!--
                Unique identifier in the model's node.
                By convention, we put the module's name in lowercase.
            -->
            <Daloradius_Deleteaccount>

                <!--
                    The path to our models directory, with directory
                    separators replaced by underscores
                -->
                <class>Daloradius_Deleteaccount_Model</class>

            </Daloradius_Deleteaccount>

        </models>

        <!-- Defining an event observer -->
        <events>
            <!-- The code of the event we want to observe -->
            <adminhtml_customer_delete>

                <!-- Defining an observer for this event -->
                <observers>

                    <!--
                        Unique identifier within the
                        checkout_onepage_controller_success_action node.
                        By convention, we write the module's
                        name in lowercase.
                    -->
                    <Daloradius_Deleteaccount>

                        <!-- The model to be instantiated -->
                        <class>Daloradius_Deleteaccount/observer</class>

                        <!-- The method of the class to be called -->
                        <method>delete_account</method>

                        <!-- The type of class to instantiate -->
                        <type>singleton</type>

                    </Daloradius_Deleteaccount>

                </observers>

            </adminhtml_customer_delete>

,
            <!-- The code of the event we want to observe -->
            <adminhtml_customers_delete>

                <!-- Defining an observer for this event -->
                <observers>

                    <!--
                        Unique identifier within the
                        checkout_onepage_controller_success_action node.
                        By convention, we write the module's
                        name in lowercase.
                    -->
                    <Daloradius_Deleteaccount>

                        <!-- The model to be instantiated -->
                        <class>Daloradius_Deleteaccount/observer</class>

                        <!-- The method of the class to be called -->
                        <method>delete_account</method>

                        <!-- The type of class to instantiate -->
                        <type>singleton</type>

                    </Daloradius_Deleteaccount>

                </observers>

            </adminhtml_customers_delete>

,
        </events>

    </global>
</config>